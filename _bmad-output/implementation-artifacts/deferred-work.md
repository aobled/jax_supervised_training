## Deferred from: code review of group_a.diff (2026-07-12)

- **[Review][Defer] Constantes de batch size dupliquées et non synchronisées** — `inference_utils.py` définit `_CLF_BATCH_SIZE = 32` et `_DET_BATCH_SIZE = 32` en privé ; `bounding_boxes_with_classification_from_video_generation.py` et `tools/audit_dataset_detection.py` définissent séparément leurs propres `BATCH_SIZE`/`CLF_BATCH_SIZE` (utilisés pour le warmup JIT et le chunking). Les valeurs concordent aujourd'hui par coïncidence, mais rien ne les lie : changer l'une sans l'autre recompilerait silencieusement ou romprait le padding anti-recompilation JIT. Risque de couplage **pré-existant** (le couplage similaire existait déjà avant le refactor, seulement localisé différemment) — relocalisé, pas introduit. Corriger proprement nécessiterait de changer la signature à 5 paramètres mandatée par AD-1 pour `decode_segmentation_and_detect_batch`/`predict_crops_batch` (paramètre `batch_size` explicite), une décision d'architecture qui dépasse le cadre de cette revue.

- **[Review][Defer] `_CROP_MARGIN_PERCENT` figée en constante privée, non exposée en paramètre** — même catégorie que ci-dessus. Valeur (0) inchangée par rapport au comportement pré-refactor (déjà une constante globale de script figée à 0, jamais appelée avec une autre valeur). Pas de régression fonctionnelle ; juste une perte de configurabilité déjà présente avant ce refactor.

- **[Review][Defer] `tools/boxes_process_manual_tkinter.py` conserve des wrappers JIT inline dupliquant `build_predict_fn`/`build_clf_predict_fn`** — `jax.jit(lambda x: det_model.apply(...))` / `jax.jit(lambda x: clf_model.apply(...))` (lignes ~1082-1083) sont un pattern pré-existant, non touché par ce diff, jamais dans le périmètre nommé de FR2/FR10 pour ce fichier (qui n'utilisait déjà pas de fonction factory nommée). Opportunité d'amélioration pour un futur cycle, pas un défaut introduit par ce refactor.
