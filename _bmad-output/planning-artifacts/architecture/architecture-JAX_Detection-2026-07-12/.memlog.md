---
scope: Module d'inférence mutualisé + nettoyage model_library.py/dataset_configs.py (refactor JAX_Detection, cf. PRD prd-JAX_Detection-2026-07-12)
purpose: build-substrate — référence technique pour l'implémentation des epics/stories à venir
altitude: feature
updated: 2026-07-12T17:40
---

- (decision by user) Mode = Coaching path (cohérent avec le PRD)
- (decision by user) Livrable = spine seul pour ce cycle (+ diagrammes ciblés sur le module d'inférence mutualisé). Vision plus large (documentation .puml de tout le code/modèles/scripts) explicitement hors scope, notée pour plus tard.
- (decision by user) Format diagrammes = Mermaid (natif au skill), pas PlantUML pour cette phase
- (direction) Investigation du code réel (les 6 fonctions divergentes, lecture complète) : get_iou et _preprocess_crop_to_hwc sont en fait IDENTIQUES partout (fusion triviale). load_jax_model diverge peu (benchmark.py a un fallback de chemin plus faible). predict_crop et non_max_suppression divergent réellement, et la divergence est corrélée à DEUX paradigmes de détection distincts : segmentation (aircraft_detector_unet, vivant) vs grid-based YOLO-like (aircraft_detector_v3/v7_advanced, morts d'après FR5).
- (question) bounding_boxes_with_classification_from_benchmark.py utilise decode_grid_and_detect, une logique de décodage entièrement liée aux modèles grid-based (v3/v7_advanced) supprimés par FR5. Sa fonction non_max_suppression(boxes, scores, iou_threshold) vectorisée numpy n'est pas une simple variante à fusionner : elle appartient à une chaîne de décodage vouée à disparaître. Question pour l'utilisateur : ce fichier doit-il être adapté au paradigme segmentation, ou considéré lui-même comme partiellement mort ?
- (direction) heatmap_generation.py et heatmap_contouring.py : load_detection_model sans aucun fallback de chemin ni re-init des batch_stats manquants (contrairement aux 3 autres fichiers) — fragilité latente identifiée, pas juste une divergence de style.
- (decision by user) bounding_boxes_with_classification_from_benchmark.py est lui-même un vestige (lié aux modèles grid-based v3/v7_advanced morts) -> suppression complète, pas de réconciliation. Conséquence : sa variante numpy-vectorisée de non_max_suppression(boxes, scores, iou_threshold) et decode_grid_and_detect disparaissent avec le fichier -> non_max_suppression n'a alors plus AUCUNE divergence réelle (une seule version list-based survit).
- (direction) Conséquence sur le PRD : FR2 listait benchmark.py parmi les 6 fichiers à faire importer depuis le module partagé — il doit rejoindre FR7/FR8 (suppression) à la place. Goal 1 (baseline/diff) passe de 6 à 5 fichiers concernés. PRD à mettre à jour pour rester synchronisé (discipline Update de bmad-architecture).
- (decision by user) predict_crop : les deux variantes coexistent dans le module partagé comme 2 fonctions publiques distinctes — predict_crop (image unique, tools/images_generation.py) et predict_crops_batch (batch/JIT, video_generation.py) — plutôt qu'un seul comportement canonique forcé.
- (decision by user) load_detection_model : version canonique unique = celle avec fallback de chemin 3 niveaux + ré-initialisation des batch_stats manquants (corrige la fragilité latente de heatmap_generation.py/heatmap_contouring.py).
- (decision) Fallback model_name par défaut dans load_detection_model : corrigé de 'aircraft_detector_v3'/'aircraft_detector_v7_advanced' (morts) vers 'aircraft_detector_unet' (vivant) — correction mécanique, pas un arbitrage.
- (decision by user) Module partagé = inference_utils.py à la racine (cohérent avec le style plat existant du projet, pas de sous-package).
- (decision by user) 3 relectures indépendantes lancées (rubric, reality-check, adversarial) : 2 critical + 3 high + plusieurs medium/low, tous confirmés par lecture directe du code, pas des faux positifs. Trouvailles majeures : decode_segmentation_and_detect vs _batch diverge réellement (jamais nommé dans AD-1 initial) ; load_classification_model/build_predict_fn (audit_dataset_classification.py) est une famille de duplication non captée par la recherche par nom identique de l'audit initial ; inference_utils.py n'avait pas d'auteur assigné entre les 5 stories parallèles ; 2 fichiers hors scope (tools/audit_dataset_detection.py, tools/boxes_process_manual_tkinter.py) importent directement depuis video_generation.py.
- (decision by user) decode_segmentation_and_detect / _batch : gardées comme 2 fonctions distinctes (même logique que predict_crop/predict_crops_batch). Confirmé après lecture complète des deux corps : pas une simple histoire de batch-size, mais un vrai compromis résolution/précision (pleine résolution+double seuil vs basse résolution+projection HD) — pas trivialement unifiable sans dégrader soit la précision de l'outil image soit le débit temps réel vidéo. Priorité explicite de l'utilisateur : le pipeline vidéo (le plus abouti, temps réel) ne doit jamais être dégradé pour unifier.
- (decision by user) tools/audit_dataset_detection.py et tools/boxes_process_manual_tkinter.py rejoignent le scope (FR2 étendu à 7 fichiers) plutôt qu'un shim de compatibilité — coût marginal, évite une casse silencieuse sans suite de tests.
- (event) spine finalized
